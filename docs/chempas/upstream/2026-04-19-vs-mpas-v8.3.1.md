# CheMPAS-A vs MPAS-Model v8.3.1 — Comparison

Date: 2026-04-19
Baseline: MPAS-Dev/MPAS-Model tag `v8.3.1` (commit `b9090a143`)
Current: CheMPAS-A `develop` (commit `4b9cdee`)

---

## Methodology

File-tree diff via `git diff --no-index` between `/tmp/mpas-model-v8.3.1` (worktree
at tag `v8.3.1`) and `/Users/fillmore/EarthSystem/CheMPAS-A` (develop HEAD). Build
artifacts (`.o`, `.mod`, `.a`, `.dSYM/`) and NetCDF files (`.nc`) excluded from all
counts. Modified files are counted by source extension (`.F`, `.f90`, `.c`, `.cmake`,
`.mk`, `.xml`, `.txt`, `.cfg`). Line-count statistics from `git diff --no-index --stat`.
For the most-changed files, the diff was spot-checked to characterize the kind of change.
The chemistry subtree is summarized at a high level because it is essentially all-new.

---

## Baseline Confirmation

CheMPAS-A is **post-v8.3.1** (i.e., the fork was taken at or after v8.3.1, not before).

| Check | Result |
|---|---|
| Every v8.3.1 source file present at same path in CheMPAS-A | 1197 / 1197 |
| v8.3.1 source files byte-identical with their CheMPAS-A counterpart | 1168 / 1197 (97.6%) |
| `src/core_atmosphere/Externals.cfg` MMM-physics tag bump (PR #1337) | byte-identical to v8.3.1 |
| `src/core_init_atmosphere/mpas_init_atm_cases.F` LBC humidity vertical-interp fix (PR #1335) | the buggy lines are absent from CheMPAS-A, matching v8.3.1's fix |

The 29 source files that differ between CheMPAS-A and v8.3.1 are intentional CheMPAS-A modifications (chemistry coupling, build wiring, the halo-interface refactor, the `mpas_dmpar.F` `pointer`→`intent(in)` fix, etc.); none of them is a regression to a pre-v8.3.1 state.

CheMPAS-A source-file count vs v8.3.1: **1219 vs 1197** — a strict superset. Zero files present in v8.3.1 are missing from CheMPAS-A.

---

## Top-level Overview

| Item | Status | Note |
|------|--------|------|
| `src/` | Modified | Core framework, atmosphere core, and build system all changed |
| `cmake/` | Modified | One file changed (`FindPnetCDF.cmake`, +6/-4 lines) |
| `docs/` | CheMPAS-A-only additions | Upstream `docs/` had no files; 40 new files added |
| `testing_and_setup/` | Identical | No changes detected |
| `README.md` | Modified | Substantially rewritten (+190/-60 lines) to describe CheMPAS-A |
| `INSTALL` | Identical | Unchanged |
| `Makefile` (top-level) | Modified | +84/-13 lines — `llvm` target + MUSICA support |
| `AGENTS.md` | CheMPAS-A-only | Agent workflow document |
| `BUILD.md` | CheMPAS-A-only | Platform-specific build instructions, including Ubuntu/GCC |
| `RUN.md` | CheMPAS-A-only | Test case execution guide |
| `CLAUDE.md` | CheMPAS-A-only | AI assistant project configuration |
| `PURPOSE.md` | CheMPAS-A-only | Project motivation |
| `PLAN.md` | CheMPAS-A-only | Development plan (likely transient) |
| `scripts/` | CheMPAS-A-only | Python visualization and utility scripts |
| `micm_configs/` | CheMPAS-A-only | MICM/MUSICA chemical mechanism YAML files |
| `test_cases/` | CheMPAS-A-only | Reference namelists and streams for idealized cases |
| `namelist.atmosphere` | CheMPAS-A-only | Working namelist for atmosphere runs |
| `namelist.init_atmosphere` | CheMPAS-A-only | Working namelist for init runs |
| `streams.atmosphere` | CheMPAS-A-only | Working streams definition |
| `streams.init_atmosphere` | CheMPAS-A-only | Working streams definition |
| `stream_list.atmosphere.*` | CheMPAS-A-only | Diagnostic and output stream lists |
| Physics table files (`*.TBL`, `*.DBL`, etc.) | CheMPAS-A-only | Runtime data files (committed to repo) |

---

## Chemistry (expected new)

The upstream v8.3.1 has no chemistry coupling — the `src/core_atmosphere/chemistry/`
path does not exist in the baseline. CheMPAS-A adds this directory entirely from scratch.
It contains 5 top-level `.F` source files and a `musica/` subdirectory with 2 `.F`
files, for 7 chemistry source files in total. The `chemistry/` path also includes a
`Makefile` and build artifacts (`.mod`, `.o`, `libchem.a`).

The top-level chemistry files implement: the generic chemistry interface
(`mpas_atm_chemistry.F`), the lightning-NOx source parameterization
(`mpas_lightning_nox.F`), the solar geometry calculation (`mpas_solar_geometry.F`),
and the TUV-x photolysis driver (`mpas_tuvx.F`). The `musica/` subdirectory contains
the MUSICA/MICM coupler (`mpas_musica.F`) and its build file.

The chemistry module is conditionally compiled via the `DO_CHEMISTRY` preprocessor
flag (set by the `MUSICA=true` build option). The coupling entry points — init, step,
and finalize — are called from `mpas_atm_core.F`; runtime chemistry tracers are now
added through the MPAS runtime-var callback registered from `mpas_atm_core_interface.F`.
A companion file `MPAS_MICM_tracer_consistency.md` (in `src/core_atmosphere/`) documents
the tracer naming conventions between MPAS and MICM.

---

## Non-chemistry Differences

### `src/framework/`

11 source files modified (out of ~40 in the directory).

| File | Change | Nature |
|------|--------|--------|
| `mpas_framework.F` | +17/-1 lines | Added MUSICA version reporting to the startup banner (`#ifdef MPAS_USE_MUSICA` block that calls `get_micm_version()` and logs the result) |
| `mpas_pool_routines.F` | +10 lines | Added `nullify(value)` / `nullify(subPool)` / `nullify(package)` guards before pool lookup calls in 6 `mpas_pool_get_config` overloads — defensive null-initialization of output pointers |
| `mpas_stream_manager.F` | +25 lines | Added error-return checks after each `MPAS_stream_list_remove` call in `MPAS_stream_mgr_remove_alarm`; previously errors were logged but execution continued |
| `mpas_halo.F` | +6 lines | Added `#ifdef NOMPIMOD` guard so `include 'mpif.h'` is used on macOS/LLVM (NOMPIMOD path) instead of `use mpi` |
| `mpas_field_routines.F` | +9 lines | Added loop-index variable declarations (`integer :: i`, `i,j`, `i,j,k`) to field allocation routines; appears to fix implicit variable declarations |
| `mpas_stream_list.F` | +9/-6 lines | Minor refactor — 15 lines changed |
| `mpas_block_creator.F` | +1 line | Added `integer :: nCells, nEdges, nVertices` local variable declaration |
| `mpas_stream_inquiry.F` | +1/-1 lines | Single-line change |
| `mpas_timekeeping.F` | +1 line | Single-line addition |
| `mpas_dmpar.F` | +1/-1 lines | Changed `integer, dimension(:), pointer` to `integer, dimension(:), intent(in)` for the `inlist` parameter of an MPI scatter helper — fixes an implicit pointer-to-intent mismatch |
| `CMakeLists.txt` | +3 lines | Minor addition |

### `src/operators/`

No source files modified. The only "changes" in a `git diff --no-index` output are
build artifacts (`.o`, `.mod`, `libops.a`) present in CheMPAS-A but not in the baseline.

### `src/core_atmosphere/` (excluding chemistry)

**Top-level core files:**

| File | Change | Nature |
|------|--------|--------|
| `mpas_atm_core_interface.F` | Runtime-var integration | Queries MICM config for species names, registers the chemistry runtime-var callback, and generates `scalars` / `scalars_tend` through `atm_generate_pools`; gated behind `#ifdef MPAS_USE_MUSICA` |
| `mpas_atm_core.F` | +69/-20 lines | Added chemistry init/step/finalize call sites (`chemistry_init`, `chemistry_seed_chem`, `chemistry_step`, `chemistry_finalize`); gated behind `#ifdef DO_CHEMISTRY` |
| `mpas_atm_halos.F` | +2/-15 lines | Replaced inline `abstract interface` block for `halo_exchange_routine` with `#include "mpas_halo_interface.inc"` — deduplication of abstract interface definition |
| `Registry.xml` | +88 lines | See dedicated section below |

**Dynamics (`src/core_atmosphere/dynamics/`):**

| File | Change | Nature |
|------|--------|--------|
| `mpas_atm_time_integration.F` | +2/-15 lines | Same abstract-interface deduplication as `mpas_atm_halos.F` — replaced inline block with `#include "mpas_halo_interface.inc"` |

**Physics (`src/core_atmosphere/physics/`):**

| File | Change | Nature |
|------|--------|--------|
| `mpas_atmphys_todynamics.F` | +2/-15 lines | Same abstract-interface deduplication (third instance) |
| `mpas_atmphys_lsm_noahmpinit.F` | +1/-1 lines | Renamed loop variable: `ns` → `n` in the NoahMP LSM init routine |
| `physics_wrf/module_cu_gf.mpas.F` | +1 line | Single-line addition |
| `physics_wrf/module_sf_urban.F` | Not found at same path in CheMPAS-A | Possibly renamed or removed; path discrepancy noted but not confirmed |

**Physics (new files) — `physics_noaa/UGWP/`:**

The entire `src/core_atmosphere/physics/physics_noaa/` subtree is absent from
upstream v8.3.1. CheMPAS-A adds a `UGWP/` subdirectory with 7 Fortran source files
implementing the CIRES Unified Gravity Wave Parameterization v1:
`bl_ugwp.F`, `bl_ugwpv1_ngw.F`, `cires_tauamf_data.F`,
`cires_ugwpv1_initialize.F`, `cires_ugwpv1_module.F`, `cires_ugwpv1_solv2.F`,
`cires_ugwpv1_triggers.F`.
These are included in the physics Makefile build via `-I./physics/physics_noaa/UGWP`.

**Diagnostics (`src/core_atmosphere/diagnostics/`):**

| File | Change | Nature |
|------|--------|--------|
| `mpas_cloud_diagnostics.F` | +3/-3 lines | Moved `public ::` declaration to after `private` — Fortran ordering fix that prevents default-private from hiding the public declarations |

### `src/core_init_atmosphere/`

| File | Change | Nature |
|------|--------|--------|
| `mpas_init_atm_cases.F` | +50/-24 lines | Added `config_specified_zeta_levels` namelist option: reads user-supplied edge heights (in metres) from a text file, enabling non-uniform vertical grids (e.g., the 60-level stretched supercell grid); falls back to the legacy 20-km uniform column when the option is empty |
| `Registry.xml` | +9 lines | Added `config_specified_zeta_levels` namelist option (character type, default empty) to the `vertical_grid` namelist record; also added 3 passive scalar vars (`qAB`, `qA`, `qB`) for tracer advection test cases |

### `src/core_landice/`, `src/core_ocean/`, `src/core_seaice/`, `src/core_sw/`

No modifications. Identical to upstream v8.3.1 in all source files.

### `src/core_test/`

| File | Change | Nature |
|------|--------|--------|
| `mpas_test_core_streams.F` | +167/-2 lines | Substantial additions to the test framework's stream testing routines |
| `mpas_test_core.F` | +14/-2 lines | Minor additions to the test core driver |

The nature of these changes was not fully inspected; they may reflect upstream
hotfix changes that were incorporated during CheMPAS-A development, or CheMPAS-A-specific
test infrastructure.

### `src/external/`

No source files modified. Identical to upstream v8.3.1.

### `src/tools/`

No source files modified. Identical to upstream v8.3.1.

### `src/driver/`

No modifications detected.

### Build System

**Top-level `Makefile`** (+84/-13 lines): Three major additions:

1. **`llvm` target** — new build target for macOS/LLVM/flang. Sets `OMPI_FC=flang`,
   `OMPI_CC=clang`, `OMPI_CXX=clang++` and passes `NOMPIMOD` (so `include 'mpif.h'`
   is used instead of `use mpi`). Exports OpenMPI wrapper variables to the shell.

2. **MUSICA support block** — `ifeq "$(MUSICA)" "true"` block that calls
   `pkg-config --cflags musica-fortran` and `pkg-config --libs musica-fortran`
   at Makefile parse time, sets `-DMPAS_USE_MUSICA`, and re-appends NetCDF libraries
   after `-lmusica` to satisfy link-order requirements for TUV-x's embedded NetCDF objects.

3. **`musica_fortran_test` target** — pre-link smoke test that compiles and links a
   minimal Fortran program against the MUSICA library; build fails fast with a helpful
   error if the library is not functional.

4. **`NETCDFF` variable support** — separate variable for a Fortran-only NetCDF
   installation (needed on macOS where Homebrew's gfortran-built `libnetcdff` is
   ABI-incompatible with flang when MUSICA is enabled).

**`src/core_atmosphere/Makefile`** (+22/-10 lines): Added `CHEMISTRY` and `CHEMCORE`
variables and a `chemcore` target. When `CHEMISTRY=-DDO_CHEMISTRY`, the build recurses
into `chemistry/` to build `libchem.a`, explodes it into `libchem/`, and links its
`.o` files into `libdycore.a`. The compiler command lines for `.F` sources now include
`-I./chemistry` so that MUSICA `.mod` files are visible.

**`cmake/Modules/FindPnetCDF.cmake`** (+6/-4 lines): Minor fix — 10 lines changed,
likely a path or variable name correction in the PnetCDF CMake finder.

**`src/framework/CMakeLists.txt`** (+3 lines): Minor addition.

---

### `Registry.xml` (key file)

**`src/core_atmosphere/Registry.xml`**: chemistry configuration records added
behind `#ifdef MPAS_USE_MUSICA`. The original single chemistry-related record
has since been split into `chemistry`, `photolysis`, and `lnox` records:

| Option | Type | Default | Purpose |
|--------|------|---------|---------|
| `config_micm_file` | character | `""` | Path to MICM configuration YAML |
| `config_chem_substeps` | integer | 1 | MICM sub-steps per MPAS outer dt |
| `config_micm_relative_tolerance` | real | 1e-6 | MICM solver relative tolerance |
| `config_chemistry_ref_solve` | logical | false | Duplicate MICM solve for advection diagnostics |
| `config_chemistry_interval` | real | 0.0 s | Operator-split chemistry interval |
| `config_chem_max_failures` | integer | 0 | Maximum tolerated chemistry failures |
| `config_tuvx_config_file` | character | `""` | Path to TUV-x config JSON (empty = fallback) |
| `config_tuvx_top_extension` | logical | false | Extend TUV-x column above MPAS domain |
| `config_tuvx_extension_file` | character | `""` | Path to upper-atmosphere climatology CSV |
| `config_tuvx_update_interval` | real | 0.0 s | TUV-x call-gating interval (0 = every step) |
| `config_j_no2_max` | real | 0.0 s^-1 | Fallback daytime NO2 photolysis rate |
| `config_chemistry_latitude` | real | 0.0 deg | Solar geometry latitude for idealized cases |
| `config_chemistry_longitude` | real | 0.0 deg | Solar geometry longitude for idealized cases |
| `config_chemistry_use_grid_coords` | logical | false | Per-cell vs broadcast solar geometry |
| `config_lnox_source_rate` | real | 0.0 | Lightning-NOx source rate (ppbv/s) |
| `config_lnox_w_threshold` | real | 5.0 m/s | Minimum w for LNOx injection |
| `config_lnox_w_ref` | real | 10.0 m/s | Reference excess w for LNOx scaling |
| `config_lnox_z_min` | real | 5000 m | Lower altitude for LNOx injection |
| `config_lnox_z_max` | real | 12000 m | Upper altitude for LNOx injection |
| `config_lnox_nox_tau` | real | 0.0 s | NOx sink timescale (0 = disabled) |

Chemistry tracer species (e.g., NO, NO2, O3) and photolysis diagnostics
(e.g., `j_jNO2`, `j_jO2`) are **not** declared in `Registry.xml`. They are
discovered at runtime from MICM/TUV-x configuration and injected dynamically by
the chemistry runtime-var callbacks.

**`src/core_init_atmosphere/Registry.xml`**: +9 lines.
Added `config_specified_zeta_levels` to the `vertical_grid` namelist record and
3 passive scalar variables (`qAB`, `qA`, `qB`) for tracer advection test cases.

---

### `docs/`

The upstream v8.3.1 `docs/` directory contains no tracked files. CheMPAS-A adds 40
new files organized into subdirectories:

| Subdirectory | Files | Content |
|---|---|---|
| `architecture/` | 2 | `ARCHITECTURE.md`, `COMPONENTS.md` |
| `guides/` | 2 | `TUVX_INTEGRATION.md`, `VISUALIZE.md` |
| `musica/` | 2 | `MUSICA_API.md`, `MUSICA_INTEGRATION.md` |
| `plans/` | 7 | Implementation plans (phase planning, agent artifacts) |
| `results/` | 2 | `BENCHMARKS.md`, `TEST_RUNS.md` |
| `superpowers/plans/` | 4 | Agent-generated implementation plans |
| `superpowers/specs/` | 4 | Agent-generated design specs |
| `users-guide/` | ~8 | Draft user guide chapters |
| `README.md` | 1 | Docs index |

No upstream `docs/` files were modified (there were none to modify).

### `testing_and_setup/`

Identical to upstream v8.3.1. No changes detected.

### Top-level new files in CheMPAS-A not in v8.3.1

Beyond `docs/`, `scripts/`, `micm_configs/`, and `test_cases/` (directories),
CheMPAS-A adds at the top level:

- `AGENTS.md`, `BUILD.md`, `RUN.md`, `CLAUDE.md`, `PURPOSE.md`, `PLAN.md` —
  project documentation and AI-assistant configuration
- `namelist.atmosphere`, `namelist.init_atmosphere` — working namelists
- `streams.atmosphere`, `streams.init_atmosphere` — working stream definitions
- `stream_list.atmosphere.{diag_ugwp,diagnostics,output,surface}` — diagnostic lists
- Physics data tables: `GENPARM.TBL`, `LANDUSE.TBL`, `NoahmpTable.TBL`,
  `OZONE_DAT.TBL`, `OZONE_LAT.TBL`, `OZONE_PLEV.TBL`, `SOILPARM.TBL`, `VEGPARM.TBL`
- Radiation data: `RRTMG_LW_DATA`, `RRTMG_LW_DATA.DBL`, `RRTMG_SW_DATA`,
  `RRTMG_SW_DATA.DBL`, `CAM_ABS_DATA.DBL`, `CAM_AEROPT_DATA.DBL`, `CCN_ACTIVATE_DATA`
- Build artifacts committed to repo: `atmosphere_model`, `init_atmosphere_model`,
  `build_tables` (executables) — these should likely be in `.gitignore`

---

## Notable Findings

- **Halo interface deduplication**: The `abstract interface` block for
  `halo_exchange_routine` was duplicated verbatim in three files in upstream v8.3.1
  (`mpas_atm_halos.F`, `mpas_atm_time_integration.F`, `mpas_atmphys_todynamics.F`).
  CheMPAS-A replaced all three with a single `#include "mpas_halo_interface.inc"`.
  This new `.inc` file is present in CheMPAS-A's `src/framework/` but absent from
  upstream — it is a CheMPAS-A-originated refactor.

- **Chemistry tracers removed from Registry.xml**: MICM species (NO, NO2, O3, etc.)
  are not declared in `Registry.xml` at all. They are discovered at runtime by scanning
  the MICM YAML config and dynamically extending the `scalars` pool. This is a
  fundamental architectural departure from the upstream approach of build-time-declared
  tracers.

- **`physics_noaa/UGWP/` is entirely new and not in upstream**: The 7-file CIRES UGWPv1
  implementation under `src/core_atmosphere/physics/physics_noaa/UGWP/` has no upstream
  counterpart in v8.3.1. It is unclear whether this came from an intermediate ACOM-dev
  fork commit or was added independently in CheMPAS-A.

- **`mpas_pool_routines.F` null-pointer guards**: The 10-line addition to
  `mpas_pool_routines.F` adds `nullify()` calls before every pool lookup return path.
  This is a correctness fix: without these, callers that check `associated(ptr)` after
  a failed lookup may see stale non-null pointers from prior use of the same variable.

- **`mpas_dmpar.F` intent change**: The `inlist` parameter in an MPI scatter helper
  was changed from `pointer` to `intent(in)`. This is a bug fix — passing a pointer
  where `intent(in)` is expected causes undefined behavior under strict compilers.

- **`mpas_stream_manager.F` error-handling hardening**: The 25-line addition adds
  early-return error checks after `MPAS_stream_list_remove` calls in alarm management.
  In upstream, list-remove failures were logged but the function continued executing
  on a corrupted state.

---

## Files Likely to Conflict on a Future Upstream Merge

These are upstream-authoritative files where CheMPAS-A has non-trivial divergence.
Any future merge of MPAS-Dev/MPAS-Model changes into CheMPAS-A will require manual
resolution for:

| File | Divergence |
|------|-----------|
| `src/core_atmosphere/Registry.xml` | Chemistry, photolysis, and LNOx namelist records |
| `src/core_atmosphere/mpas_atm_core_interface.F` | Runtime-var pool generation and chemistry callback registration |
| `src/core_atmosphere/mpas_atm_core.F` | Chemistry init/step/finalize hooks inserted throughout |
| `src/core_atmosphere/Makefile` | Chemistry build target, `CHEMISTRY` variable, `-I./chemistry` flags |
| `Makefile` (top-level) | `llvm` target, MUSICA block, `musica_fortran_test`, `NETCDFF` variable |
| `src/framework/mpas_framework.F` | MUSICA version reporting in startup banner |
| `src/framework/mpas_pool_routines.F` | Null-pointer guards (low-conflict risk, small changes) |
| `src/framework/mpas_halo.F` | `NOMPIMOD` guard for macOS MPI include |
| `src/core_atmosphere/dynamics/mpas_atm_time_integration.F` | Halo interface `.inc` refactor |
| `src/core_atmosphere/physics/mpas_atmphys_todynamics.F` | Halo interface `.inc` refactor |
| `src/core_atmosphere/mpas_atm_halos.F` | Halo interface `.inc` refactor |
| `src/core_init_atmosphere/Registry.xml` | `config_specified_zeta_levels` + passive scalar vars |
| `src/core_init_atmosphere/mpas_init_atm_cases.F` | Custom vertical level reading (+50/-24 lines) |
| `README.md` | Substantially rewritten for CheMPAS-A identity |
